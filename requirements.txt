# Core LLM and Langchain
anthropic==0.29.0 # Pinned version compatible with older httpx potentially
langchain==0.1.20 # Slightly older langchain version
langchain-core==0.1.52 # Version compatible with langchain 0.1.20
langchain-openai==0.1.17 # Version compatible with langchain 0.1.20
openai==1.14.3 # Pinned version
tiktoken==0.7.0 # Keep original tiktoken version for now

# Pinecone
pinecone-client>=3.0.0,<4.0.0 # Keep flexible as before, seems okay
langchain-pinecone==0.1.1 # Match langchain ecosystem better

# AWS
boto3==1.34.141 # Specific boto3 version

# Utilities
python-dotenv==1.0.1
argparse>=1.4.0
typing-extensions>=4.7.0 # Allow slightly newer typing
tenacity>=8.2.0,<9.0.0 # Add upper bound based on conflict error
numpy>=1.24.0
loguru>=0.5.0 # Keep as is
pydantic<2.0.0 # Keep constraint as requested originally
schedule>=1.2.0 # Keep as is

# Dependencies potentially pulled by others, pin if needed later
# httpx==0.25.2 # Example if specific httpx needed for anthropic 0.29.0
# langsmith... # langsmith wasn't originally listed, let langchain-core manage it

# Removed less critical/potentially conflicting version specs if managed by others:
# langsmith-0.0.87 (Let langchain-core handle)
# dnspython>=2.0.0 (Usually not needed directly)